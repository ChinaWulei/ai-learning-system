services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: ai_learning
      MYSQL_USER: learning
      MYSQL_PASSWORD: learning123
      MYSQL_ROOT_PASSWORD: root123
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot123"]
      interval: 5s
      timeout: 5s
      retries: 20

  ai-service:
    build: ./ai-service
    environment:
      LLM_PROVIDER: ${LLM_PROVIDER:-mock}
      LLM_MODEL: ${LLM_MODEL:-mock-learning-model}
      LLM_API_KEY: ${LLM_API_KEY:-}
      LLM_BASE_URL: ${LLM_BASE_URL:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      GEMINI_TTS_MODEL: ${GEMINI_TTS_MODEL:-gemini-3.1-flash-preview-tts}
      GEMINI_TTS_VOICE: ${GEMINI_TTS_VOICE:-Sulafat}
      USE_BGE: ${USE_BGE:-false}
      JAVA_BASE_URL: http://backend:8080
      JAVA_INTERNAL_TOKEN: change-me
    expose:
      - "8000"
    volumes:
      - rag-data:/app/data

  neo4j:
    image: neo4j:5-community
    environment:
      NEO4J_AUTH: neo4j/learning123
      NEO4J_server_memory_heap_initial__size: 256m
      NEO4J_server_memory_heap_max__size: 512m
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - neo4j-data:/data
    healthcheck:
      test: ["CMD-SHELL", "cypher-shell -u neo4j -p learning123 'RETURN 1'"]
      interval: 10s
      timeout: 5s
      retries: 20

  backend:
    build: ./backend
    depends_on:
      mysql:
        condition: service_healthy
      ai-service:
        condition: service_started
      neo4j:
        condition: service_healthy
    environment:
      DB_URL: jdbc:mysql://mysql:3306/ai_learning?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      DB_USERNAME: learning
      DB_PASSWORD: learning123
      DB_DRIVER: com.mysql.cj.jdbc.Driver
      AI_SERVICE_URL: http://ai-service:8000
      JAVA_INTERNAL_TOKEN: change-me
      NEO4J_ENABLED: "true"
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USERNAME: neo4j
      NEO4J_PASSWORD: learning123
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "8081:80"

volumes:
  mysql-data:
  rag-data:
  neo4j-data:

